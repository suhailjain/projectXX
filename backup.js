/*  <View styles={styles.row}>
  <Button
  icon={{ name: 'note' }}
  backgroundColor='#003366'
    title='Surveys'
    onPress={() => {
      if (loginCheck()) {
      props.drawerState(true);
      Actions.feedback();
    } else {
      Alert.alert('login is must');
    }
  }}
  /> */
